
// This type for the Cart Item I want to display not the one used in backend
export interface CartItem {
    productId: string;
    title: string;
    unitPrice: number;
    quantity: number;
    productImageUrl: string;
}